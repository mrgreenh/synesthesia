"use strict";

define(function () {
    return {
        EVENTS: {
            TIME: {
                INCREMENT: "event:time:increment"
            } },
        INPUTS: {
            SOURCE_PARAMETERS: ["value", "intensity", "velocity"]
        }
    };
});